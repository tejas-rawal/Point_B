%p#notice= notice

%p
  %img{src: @destination.album.url(:large)}
%h3
  = "#{@destination.city}, #{@destination.country}"
%p
  = @destination.description

%p
  %b Tags:
  = @destination.category
- if @user == current_user
  = link_to 'Edit', edit_user_destination_path(@user.id, @destination)
